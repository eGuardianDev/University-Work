

задача за настаняването shifts problem
lockbox program
транспортна задача

Двойнствената задача влиза в контролното

ако няма ограничение на x1>0, тогава първата редица в
допустимото множество на двойната задача ще е = (равно), а не >= (по-голямо или равно)


ако не се знае пишем - не се знае ( трябва да се сметне )
