# From before:

- "+" са само информативни
- "%" е .....

- "=, >, <" от двете страни няма аритметика, когато знаем двете страни
	- N > 0, то работи само при 
- "=:=" - това няма да го ползваме
## is 
- `x is y + 1`
- y - трябва да го знаем
##### допълнителна библиотека
тя ни лишава този проблем
# CLP(FD) Library
`:- use_module(library(clpfd)).`
## #=, #<, #>, #<=, #>= 
```
X #< 5   -> X in inf, ..., 4
X #> 2   -> X in      3,4
label([x])  (ако интервалите са крайни) -> x = 3, x = 4
```
Понеже това е много мощно, ще забравим от "From before" нещата.
Библиотеката, която ползваме 

```
X #> 0, Y #> 0, X + Y /= 5
```

### ex 1

```
element([X|_], X).
element([_|T], X) :- element(T,X).
```
Тук празния списък е дъно и той винаги няма елементи, следователно е грешно и връща грешка (a.k.a. няма елементи)



#### Това повче няма да се ползва
```
=
=:=
is
```




## Другата седмица - graph

## за изпита - implementation of data structure via lists

list of lists, find all K same elements of K other lists