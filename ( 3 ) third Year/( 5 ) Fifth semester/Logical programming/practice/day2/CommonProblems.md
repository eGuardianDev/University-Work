


when using
` is `
RHS must be fully known



up and down computation

down uses
`N>0` for example and is 
`N>0, call compute is, call function`
but the up uses
`call function, computer "is"`

in down, we can only check if N is correct 
in up, we can generate N



#= works in both directions and is safer?


Constraints first, generators later
